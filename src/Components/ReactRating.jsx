import React, { useState } from 'react';

const ReactRating = () => {
    const [value,setValue] = useState(0)
    return (
        <div>
            
        </div>
    );
};

export default ReactRating;